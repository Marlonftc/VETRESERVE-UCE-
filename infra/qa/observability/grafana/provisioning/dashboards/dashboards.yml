apiVersion: 1

providers:
  - name: "VetReserve QA"
    orgId: 1
    folder: "VetReserve QA"
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
