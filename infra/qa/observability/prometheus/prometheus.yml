global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "reporting-analytics-service"
    metrics_path: /metrics
    static_configs:
      - targets: ["vetreserve-qa-reporting-analytics:8010"]

  # (Opcional) si luego expones /metrics en otros microservicios:
  # - job_name: "notification-service"
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ["vetreserve-qa-notification:800X"]
